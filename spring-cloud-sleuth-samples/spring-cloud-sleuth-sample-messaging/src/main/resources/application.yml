server:
  port: 3381

spring:
  application:
    name: testSleuthMessaging
  sleuth:
    log:
      json:
#        enabled: true

logging:
  pattern:
    level: '%clr([trace=%X{X-Trace-Id:-},span=%X{X-Span-Id:-}]){yellow} %5p'

sample:
  zipkin:
#   enabled: false
